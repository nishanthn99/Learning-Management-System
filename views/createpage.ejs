<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="_csrf" content="<%=_csrf%>" />
  <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet" />
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/css/styles.css" />
  <title>Add Page</title>
</head>
<body>
  <%- include("header.ejs")%>
  <div class="container mx-auto mb-5">
    <div class="flex flex-col space-y-4 mt-3">
      <div class="w-full">
        <form action="/course/<%=courseId%>/chapter/<%=chapter.id%>/page" method="post" class="editor-form">
          <input type="hidden" name="_csrf" value="<%=_csrf%>" />
          <div class="w-full">
            <label class="block text-lg font-medium mb-2">Select Chapter</label>
            <select name="chapterId" class="w-full p-2 border border-gray-300 rounded-md text-sm">
              <% for (c of chapters) { %>
                <option value="<%= c.id %>" class="text-sm font-medium">
                  <%= c.chaptertitle %>
                </option>
              <% } %>
            </select>
          </div>
          <div class="mt-4">
            <label for="title" class="block text-lg font-medium mb-2">Give Title to Page</label>
            <input type="text" name="title" placeholder="Enter Page Title" class="w-full p-2 border border-gray-300 rounded-md" />
          </div>
          <div class="mt-4">
            <label for="content" class="block text-lg font-medium mb-2">Add Content to Page</label>
            <div id="editor" class="border border-gray-300 rounded-md h-72 p-2"></div>
            <input type="hidden" id="editorInput" name="content" />
          </div>
          <div class="mt-4">
            <button class="bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700">Add Page</button>
          </div>
        </form>
      </div>
    </div>
  </div> 
  <div class="flex bg-blue-200 mx-auto p-2 rounded-md"> <%-include('notification') %></div> 
    <script src="https://cdn.quilljs.com/1.3.6/quill.js"></script>
    <script src="/js/script.js"></script>
    <%-include("footer.ejs")%>
</body>

</html>