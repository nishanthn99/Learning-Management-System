<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Page</title>
    <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
    <div class="grid grid-cols-12 mt-3 ml-4">
        <h3 class="col-span-12 text-xl font-bold"><%=course.coursetitle%></h3>
        <h4 class="col-span-12 text-lg font-semibold mt-1"><%=chapter.chaptertitle%></h4>
        <hr class="col-span-12 my-2 border-gray-300" />
        
        <div class="col-span-12">
          <form
            action="/course/<%=course.id%>/chapter/<%=chapter.id%>/page/<%=page.id%>/updatepage"
            method="post"
            class="space-y-4"
          >
            <input type="hidden" name="_csrf" value="<%=_csrf%>" />
      
            <!-- Title Input -->
            <div class="mt-2">
              <label for="PageName" class="block text-lg font-medium mb-1">Edit Title of the Page</label>
              <input
                type="text"
                name="PageName"
                value="<%=page.pagetitle%>"
                class="block w-full p-2 border border-gray-300 rounded-lg text-base"
              />
            </div>
      
            <!-- Content Editor -->
            <div class="mt-2">
              <label for="Content" class="block text-lg font-medium mb-1">Content</label>
              <div id="editor" class="border border-gray-300 rounded-lg p-2 h-72">
                <%- page.content %>
              </div>
              <input type="hidden" id="editorInput" name="Content" />
            </div>
      
            <!-- Submit Button -->
            <div class="mt-3 mb-5 flex justify-center">
              <button class="bg-yellow-500 text-white px-4 py-2 rounded-lg hover:bg-yellow-600">Edit</button>
            </div>
          </form>
        </div>
      </div>
      
      <script src="https://cdn.quilljs.com/1.3.6/quill.js"></script>
      <script src="/js/script.js"></script>
</body>
</html>      